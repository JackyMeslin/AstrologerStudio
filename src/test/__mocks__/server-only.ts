/**
 * Mock for Next.js server-only package
 *
 * The server-only package is a Next.js convention that throws an error
 * when imported in client-side code. For testing, we mock it as an empty module.
 */
export {}
