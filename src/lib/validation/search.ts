/**
 * Centralized search validation constants
 *
 * Single source of truth for search requirements.
 * Used by city search in geonames, location fields, and relocation dialogs.
 */

/** Minimum characters required to trigger a city/location search */
export const MIN_SEARCH_LENGTH = 2
